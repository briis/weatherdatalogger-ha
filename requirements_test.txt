# Pinned to a matching pair — pytest-homeassistant-custom-component's
# homeassistant pin (see its own requirements) must always equal the
# homeassistant== line below, or fixtures/test doubles drift out of sync
# with the real integration environment. Bump both together; Dependabot
# (.github/dependabot.yml) opens a PR whenever a new pair ships upstream.
homeassistant==2026.7.1
pytest-homeassistant-custom-component==0.13.345
pytest-asyncio
ruff
