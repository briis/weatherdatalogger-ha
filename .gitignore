# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/
venv/
.venv/

# Home Assistant dev instance state — everything here is regenerated by
# `hass -c config` on first run; only configuration.yaml is meant to be tracked.
config/.storage/
config/.cloud/
config/home-assistant.log*
config/home-assistant_v2.db*
config/tts/
config/blueprints/
config/deps/
config/*.log

# Editor / OS
.DS_Store
